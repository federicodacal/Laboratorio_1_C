/**
Mostrar 10 repeticiones con números ASCENDENTE, desde el 1 al 10.
**/

#include <stdio.h>
#include <stdlib.h>

int main()
{
    int contador;
    contador=0;

    while(contador<10){
        contador++;
        printf("%d\n",contador);
    }

    return 0;
}
